This simple Login template use a database which has a table 'identifiants' structured like this :

+----------+----------+----------+
| id       | login    | password |
+----------+----------+----------+

'id' must be auto increment, and 'login' & 'password' must be varchar(20).
